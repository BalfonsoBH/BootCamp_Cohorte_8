import {sumar, multplicar, mostraresult} from './calculadora.js'

let resultado = sumar(10,20);
console.log(resultado);

let resultadoMult = multplicar(12, 46);

mostraresult('multiplicacion', 12, 46, resultadoMult);